## 1. Shim script (`claude-node-launch`)

- [x] 1.1 Add the shim as a chezmoi `executable_` source at a dedicated shims dir that does not collide with `~/.local/bin/claude` (e.g. `dot_local/bin/shims/executable_claude` → `~/.local/bin/shims/claude`)
- [x] 1.2 Implement the no-`.nvmrc` fast path: builtin walk-up (`${d%/*}`, no `dirname` forks); if nothing found above and below, `exec "$HOME/.local/bin/claude" "$@"` unchanged
- [x] 1.3 Implement the up-then-down `.nvmrc` finder: walk up to filesystem root; if none, bounded walk-down skipping `node_modules/` and `.git/`; on >1 match pick shallowest and warn to stderr
- [x] 1.4 Implement PATH-prepend activation for installed versions: exact pin (`24.12.0`/`v24.12.0`) and major-prefix (`24` → highest matching `$NVM_DIR/versions/node/v24*`), prepend `…/bin` to `$PATH`, no `nvm.sh` sourcing
- [x] 1.5 Implement the cold/alias fallback: when the resolved version is not installed, or `.nvmrc` is an alias (`lts/*`, `node`, `stable`), source `nvm.sh` + `nvm install`/`nvm use`, then delegate
- [x] 1.6 Delegate via `exec "$HOME/.local/bin/claude" "$@"` by absolute path (no recursion); confirm exit status passes through

## 2. PATH wiring

- [x] 2.1 In `dot_zshrc.tmpl`, prepend the shims dir to `$PATH` ahead of `~/.local/bin` (document why ordering matters; no-op if bypassed) — _live: `$path` lists `~/.local/bin/shims` immediately ahead of `~/.local/bin`._
- [x] 2.2 `chezmoi apply` and confirm `command -v claude` resolves the shim, not `~/.local/bin/claude` — _Verified live 2026-06-30: fresh interactive zsh → `command -v claude` = `~/.local/bin/shims/claude`. **Caveat:** this was masked by an unrelated #159 regression — the new `md()` function collided with oh-my-zsh's `alias md='mkdir -p'` (`lib/directories.zsh`), a parse error at `~/.zshrc:232` that aborted sourcing **before** this PATH line (332). Fixed by `unalias md 2>/dev/null` before the function in `dot_zshrc.tmpl`; re-applied; then 2.2 passed._

## 3. AoE integration

- [x] 3.1 Verify whether AoE supports an agent-command override for the `claude` tool (inspect `aoe agents` / config schema, mirror the `[tools.lazygit].command` precedent) — AoE 1.9.5 exposes `aoe add --cmd-override <CMD>` and `--tool <named-custom-agent>` (CLI-level override). More importantly `[sandbox].enabled_by_default = false`: by default claude runs outside the sandbox and inherits the launch shell's PATH, so the shim is hit with no override needed; `NVM_DIR` passthrough is the baseline for when the sandbox is enabled.
- [x] 3.2 Add `NVM_DIR` to AoE `[sandbox].environment` in the chezmoi-managed AoE config (`private_dot_agent-of-empires/…`)
- [x] 3.3 Confirm the shim resolves the version inside AoE's sandbox (i.e. `~/.nvm` is readable there); if filesystem-isolated, document the limitation — _Resolved as **documented limitation** (2026-06-30). Default path works: `[sandbox].enabled_by_default = false` (verified in live `~/.agent-of-empires/config.toml`) → claude runs on host, inherits PATH, shim fires (proven by 2.2 + 4.x). Sandbox-on is a **Docker container** (`default_image = ghcr.io/agent-of-empires/aoe-sandbox`): host `~/.nvm` is **not** mounted (`extra_volumes = []`) and the host shim (`~/.local/bin/shims/claude`) isn't in the image, so the shim is **host-only** — inside the sandbox `NVM_DIR` passthrough alone is necessary-but-insufficient (would also need `~/.nvm` mounted + shim in-image). Docker not running locally → no live sandbox-on run; behaviour established by config analysis._

## 4. Verification

> Verification §4 deferred to post-merge (user choice). All cases below were proven against a sandboxed test harness (fake `$HOME`/`$NVM_DIR`/real-claude stub, 15 scenarios); what remains is confirming the same behavior in a live, shim-launched claude after `chezmoi apply`.

- [x] 4.1 Pinned-version worktree: launch claude via `wsc`/`wt switch -x claude` in a worktree whose `.nvmrc` pins a non-default version; confirm a subprocess `node --version` reports the pinned version — _Verified live 2026-06-30 (real installed shim, observer stub as delegation target; v20.20.2 + v24.18.0 installed, ambient v24.18.0). exact `v24.18.0`→v24.18.0; major `24`→v24.18.0; bare `20`→**v20.20.2** (real override of ambient); walk-up from `a/b/c` (.nvmrc=20 at root)→v20.20.2. Child's `command -v node` = the pinned `bin/` each time._
- [x] 4.2 Subdir case: worktree with `.nvmrc` only in `apps/web/`; confirm the downward search selects it — _Verified live: cd repo root with `.nvmrc` only in `apps/web/` (=20) → walk-down found it → child node v20.20.2. Multi-match (`deep/.nvmrc`=24, `deep/deeper/.nvmrc`=20) → shallowest (`deep`) chosen + stderr warning printed._
- [x] 4.3 Non-Node dir: confirm passthrough with no `$PATH` change and negligible overhead — _**Fixed + verified live 2026-06-30.** The walk-down is now anchored at the repo root (`git rev-parse --show-toplevel`) and only runs inside a repo. Live: launching from `~/WebstormProjects` (not a repo, 8 sibling `.nvmrc` below) → **passthrough, ambient v24.18.0**, no tree scan, no wrong-version cold-install. `~/WebstormProjects/dotfiles` (repo, no `.nvmrc`) → passthrough. The previous arbitrary `kinkoi-project`→`20.12.2` misfire no longer occurs._
- [x] 4.4 Cold install: a pinned version not yet installed triggers the one-time `nvm install` fallback, then steady-state fast path — _Verified live: pinned `20` (uninstalled) → shim sourced nvm.sh, ran `nvm install` → installed **v20.20.2** (9 s one-time); child ran under v20.20.2. Re-run with v20 now installed → fast PATH-prepend path, no nvm.sh (4.1)._
- [x] 4.5 Re-run the prelude benchmark on the final shim to confirm steady-state overhead stays in the single-digit-ms range — _**Re-benchmarked 2026-06-30 after the walk-down fix.** Fast path (installed pin, walk-up hit) = **+~9 ms** over the bare-process floor ✓ (unchanged — the fix doesn't touch the walk-up path). Passthrough from the old bug dir (`~/WebstormProjects`, not a repo) = **+~15 ms** (a single `git rev-parse` that fails fast, no tree scan) — down from the previous **~170–370 ms**. In-repo with no `.nvmrc` runs `git rev-parse` + one bounded `find` within the repo (pruned), single-to-low-double-digit ms. The "near-zero-cost passthrough" requirement now holds in the real environment._

> **Finding — RESOLVED 2026-06-30.** _Original defect: the walk-down ran `find $PWD -maxdepth 4`, so launching above a tree of sibling projects (`~/WebstormProjects`) scanned them all and activated an **arbitrary** unrelated project's `.nvmrc` (`kinkoi-project`→`20.12.2`), even cold-installing it._ **Fix applied:** the walk-down is now anchored at the **repository root** (`git rev-parse --show-toplevel`) and runs only inside a git repo; on **0 or >1 matches it passthroughs** (warning on >1) instead of guessing. This keeps the supported case — a polyglot monorepo with a single `.nvmrc` in a subdir like `apps/web/` — working from the repo root *and* from any subdir (git-root anchor, not `$PWD`), while a non-repo launch dir or an ambiguous repo falls back to ambient Node. Verified live across 7 scenarios (root pin / subdir-only / launch-from-sibling-subdir / two-subdir-ambiguous / non-repo / in-repo-no-nvmrc). Spec (`Version file discovery walks up then down`, `Absent .nvmrc is a near-zero-cost passthrough`) and design D4 updated to match.

## 5. Docs

- [x] 5.1 Update `docs/manual.html` (via the manual skill) to document the shim, PATH ordering, and the AoE `NVM_DIR` passthrough
