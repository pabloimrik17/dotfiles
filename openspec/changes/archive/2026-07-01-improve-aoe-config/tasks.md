## 1. Pre-implementation verification (resolve design Open Questions)

- [x] 1.1 Run `aoe add --help` flows + a real run: confirm `aoe add .` (no `-l`) registers a worktrunk-created worktree as a session WITHOUT creating a second worktree, and that under `wt … -x 'aoe add .'` control returns to gh-dash (exec model). — `--help` confirms: no `-w` ⇒ no worktree created (registers cwd), no `-l` ⇒ returns immediately. Live exec-model run folded into 8.4 (soak).
- [x] 1.2 Confirm whether AoE re-serializes/reorders the whole `~/.claude/settings.json` after the baked hooks land; capture the key order it writes so the template can match it (minimize `chezmoi diff`). — RESOLVED by reading the live `~/.claude/settings.json` (AoE had already injected its 5 hooks). Finding: AoE **sorts every object key alphabetically at all depths** — top-level (`alwaysThinkingEnabled, effortLevel, enabledPlugins, env, extraKnownMarketplaces, hooks, permissions, skipAutoPermissionPrompt, skipDangerousModePermissionPrompt, statusLine, voiceEnabled`), `hooks` events (`ElicitationResult, Notification, PreCompact, PreToolUse, SessionStart, Stop, UserPromptSubmit`), and nested objects (`enabledPlugins`/`extraKnownMarketplaces`+`source`, `permissions`→`allow,defaultMode,deny`, `statusLine`→`command,type`, hook entry→`hooks` before `matcher`, command obj→`command` before `type`). Arrays keep their order. AoE **omits `matcher` on its own injected hooks** but preserves `matcher: ""` on the pre-existing `bd prime` hooks. 2-space indent, trailing newline. (Feeds 3.3.)
- [x] 1.3 Audit `Alt+g` against ghostty, tmux, and AoE built-in bindings — confirm no collision before shipping `[tools.lazygit]`. — ghostty binds only `super+*` (free); tmux default prefix, no `M-g`/Alt root binding (free); lazygit present at /usr/local/bin/lazygit. AoE built-in overlay is the only residual check (folded into 7.2).
- [x] 1.4 Confirm the gh-dash PR template variables used render correctly (`{{.PrNumber}}`, `{{.RepoName}}`, `{{.RepoPath}}` proven; verify `{{.Title}}`) and decide group depth (`reviews/{{.RepoName}}` → `reviews/owner/repo`). — `{{.RepoName}}` already renders as `owner/repo` in existing `b`/`t` bindings, so `reviews/{{.RepoName}}` = `reviews/owner/repo` (depth accepted). `{{.Title}}` was DROPPED from `f` (shell-injection risk, CodeRabbit PR #152) → only `{{.PrNumber}}`/`{{.RepoName}}`/`{{.RepoPath}}` remain, all proven.
- [x] 1.5 Identify a TOML-merge tool already on PATH for the `modify_` script. — SUPERSEDED (D3): the earlier "none present → static fallback" verdict missed `uv` (already in `BREW_PACKAGES`). `uv run --with tomlkit` provisions `tomlkit` ephemerally with **no new brew dep**. tomlkit is style-preserving (1:1 round-trip), which `dasel`/`yq` are not — only style preservation keeps `chezmoi diff` quiet across AoE rewrites. Governs Group 4 (now built, not fallback).

## 2. SHIP — AoE config knobs

- [x] 2.1 Add `[tmux].clipboard = "enabled"` to `private_dot_agent-of-empires/private_config.toml` (OSC 52 passthrough for wrapped agents).
- [x] 2.2 Add `[tmux].mouse = "disabled"` (determinism; user owns `~/.tmux.conf`).
- [x] 2.3 Add `[status_hooks].on_error` invoking `terminal-notifier` with a distinct sound (e.g. `-sound Basso`), mirroring `on_waiting`/`on_idle`.
- [x] 2.4 `chezmoi apply` (or dry-run) and confirm the three keys render and the file stays `0600`. — DONE (post-merge, source synced to `bd8750a`). `chezmoi cat ~/.agent-of-empires/config.toml` renders `clipboard = "enabled"`, `mouse = "disabled"`, and `on_error = '… -sound Basso'` under `[tmux]`/`[status_hooks]`; live file mode is `-rw-------` (0600). Verified read-only via `chezmoi cat` + `stat`.

## 3. SHIP — Bake AoE status hooks into the Claude settings template

- [x] 3.1 Add the five AoE session-status hooks to the `hooks` block of `dot_claude/settings.json.tmpl`: `UserPromptSubmit`/`PreToolUse`/`ElicitationResult` → `running`, `Notification` (matcher `permission_prompt|elicitation_dialog`) → `waiting`, `Stop` → `idle`; each `sh -c '[ -n "$AOE_INSTANCE_ID" ] || exit 0; mkdir -p /tmp/aoe-hooks/$AOE_INSTANCE_ID 2>/dev/null; printf <status> > /tmp/aoe-hooks/$AOE_INSTANCE_ID/status 2>/dev/null; exit 0'`. — Template renders to valid JSON; self-gate functionally tested (unset ⇒ no-op exit 0; set ⇒ writes status).
- [x] 3.2 Keep the existing `SessionStart`/`PreCompact` `bd prime` hooks intact.
- [x] 3.3 Apply the key order learned in 1.2 so `chezmoi diff` is clean after the next `aoe` launch. — UNBLOCKED (1.2 done) but deferred to post-merge QA. Recipe: rewrite `settings.json.tmpl` so the RENDERED JSON matches AoE's full-alphabetical key order at every depth (incl. `enabledPlugins`/`extraKnownMarketplaces` entries with the `{{ if }}` conditionals slotted into their alphabetical position), omit `matcher` on AoE's injected hooks (keep `matcher: ""` on `bd prime`), `command` before `type`, `hooks` before `matcher`. Verify by `chezmoi --source <clone> execute-template` rendering == the live AoE-serialized file, then launch `aoe` once and confirm `chezmoi diff` is empty (only achievable live → post-merge). NOTE: "accept one-time reformat" does NOT reach a quiet steady state (apply→AoE re-sort→apply thrash), so the full alphabetical rewrite is the real fix. — DONE (post-merge). Rewrote `dot_claude/settings.json.tmpl` so its rendered output is byte-identical to AoE's canonical serialization: (a) full-alphabetical key order at every depth (top-level, `enabledPlugins`, `extraKnownMarketplaces` + nested `source`, `hooks` events, `permissions`, `statusLine`), (b) `command` before `type`, `hooks` before `matcher`, (c) `matcher` omitted on the 4 empty-matcher AoE hooks (`UserPromptSubmit`/`PreToolUse`/`ElicitationResult`/`Stop`), `matcher: ""` kept on `bd prime` (`SessionStart`/`PreCompact`), real matcher on `Notification`, (d) NO trailing newline (AoE writes none), (e) `superwhisper` entries slotted into their alphabetical position inside the `{{ if darwin/arm64 }}` conditional. VERIFIED via `chezmoi --source <devclone>`: `diff ~/.claude/settings.json` is QUIET and `cmp` reports 0 byte differences vs the live file; the forced-true arm64 branch renders valid JSON with `superwhisper` correctly placed (alphabetical order preserved in both tables). Note: this machine is `darwin/amd64`, so `superwhisper` does not render here (matches live).
- [x] 3.4 `chezmoi apply`, launch `aoe`, confirm the status column works and `~/.claude/settings.json` no longer thrashes against the template. — DONE (post-merge, live). `chezmoi update` applied the config; launched `aoe` with a live Claude session. Status column works (TUI shows the active session distinct from idle ones; Catppuccin Mocha theme renders). Baked hooks proven firing: `/tmp/aoe-hooks/<instance>/status` = `running` for the active session. Anti-thrash proven: after `aoe` rewrote/re-sorted both files, `chezmoi diff ~/.claude/settings.json` and `chezmoi diff ~/.agent-of-empires/config.toml` are both QUIET.

## 4. config.toml writeback preservation (modify_ merge via tomlkit — D3)

- [x] 4.1 Replace static `private_dot_agent-of-empires/private_config.toml` with `private_dot_agent-of-empires/modify_private_config.toml`: a `modify_` script (`uv run --with tomlkit python`) that overlays only the managed keys (a `MANAGED` `(table-path → value)` list) onto the on-disk TOML via check-then-set, preserving `[app_state]`/`[web]`/`[cockpit]`/`[logging]` and default-expansions. Before deleting the static file, assert `MANAGED` is a superset of its keys (17/17, identical values — no key loss); `git rm` the static file in the same commit. — DONE. `modify_private_config.toml` ships a `sh` wrapper → `uv run --with tomlkit python` merge over a 17-entry `MANAGED` list (check-then-set), super-table creation for `[tools.lazygit]`; static file `git rm`'d. No-key-loss asserted programmatically: the managed-only baseline flattens to exactly the static file's 17 keys, identical values, zero missing/mismatched/extra.
- [x] 4.2 Fidelity + idempotency: check-then-set (skip-if-equal → no formatting churn); `[status_hooks]` commands written as literal (single-quote) strings to match AoE; empty stdin → managed-only baseline; `uv`-absent → passthrough/baseline fallback (self-heals next apply); `0600` via the `modify_private_` prefix. — DONE, all exercised against the real live config: check-then-set skips equal keys (preserves AoE's multiline `sandbox.environment` byte-for-byte); `on_waiting`/`on_idle`/`on_error` emitted via `tomlkit.string(..., literal=True)` (single-quoted, no `\"` churn); empty stdin → 17-key baseline (idempotent); `uv` absent → live config passed through unchanged (empty→empty); `0600` from the `modify_private_` prefix inside the `private_` (0700) dir.
- [x] 4.3 Verify on the real live config: A (`dumps(parse(x)) == x`, byte 1:1), B (`merge(merge(x)) == merge(x)`, idempotent → quiet re-apply), C (`[app_state]`/`[web]`/`[cockpit]`/`[logging]` + `last_browse_dir` preserved), and managed keys forced. Live (soak): `chezmoi apply`, launch AoE, confirm `chezmoi diff` stays quiet after AoE rewrites the file. — A/B/C + managed-forced VERIFIED against the actual `~/.agent-of-empires/config.toml` (all PASS): A byte-identical round-trip; B second merge byte-identical; C all runtime tables + `last_browse_dir` preserved; managed keys forced (`theme.name` empire→mocha, `tmux.mouse/clipboard` auto→disabled/enabled, `sound` false→true, `+on_error` literal, `+[tools.lazygit]`). Quietness proven: after a simulated AoE runtime-key rewrite the merge leaves the file byte-identical. `chezmoi --source <devclone> cat` reproduces the merge exactly. Remaining (soak): real `chezmoi apply` + live AoE rewrite → confirm `chezmoi diff` stays quiet — deferred to post-merge (source not yet synced to `~/.local/share/chezmoi`). — DONE (post-merge): live `chezmoi diff ~/.agent-of-empires/config.toml` shows ONLY the 6 expected managed deltas (`theme.name` empire→catppuccin-mocha, `tmux.mouse` auto→disabled, `tmux.clipboard` auto→enabled, `sound.enabled` false→true, `+on_error`, `+[tools.lazygit]`); every runtime table (`[app_state]`/`[web]`/`[cockpit]`/`[logging]`/`[logging.targets]`) preserved. Post-apply re-quiet guaranteed by the already-proven idempotency (test B).

## 5. TRIAL — Catppuccin Mocha theme

- [x] 5.1 Scaffold via `aoe theme export tokyo-night-storm`, swap in Mocha hexes (base `#1e1e2e`, blue `#89b4fa`, green `#a6e3a1`, yellow `#f9e2af`, red `#f38ba8`, surface0 `#313244`); save as `private_dot_agent-of-empires/themes/catppuccin-mocha.toml`. — Authored from exported schema; full Mocha palette mapped; parses OK.
- [x] 5.2 Set `[theme].name = "catppuccin-mocha"`; `aoe` renders it correctly. — Name set in config; live visual render is a soak check.

## 6. TRIAL — Sound

- [x] 6.1 `aoe sounds install`; set `[sound].enabled = true`. — Installed (10 sounds in `~/.agent-of-empires/sounds`); `[sound].enabled = true` set in config.
- [x] 6.2 Add the AoE sounds dir to `.chezmoiignore` (assets are not committable). — Created `.chezmoiignore` with `.agent-of-empires/sounds` (+ `/**`).

## 7. TRIAL — lazygit tool-session

- [x] 7.1 (Gated on 1.3) Add `[tools.lazygit]` with `command = "lazygit"` and `hotkey = "Alt+g"`. — Added (subtable kept last in private_config.toml).
- [x] 7.2 Confirm `Alt+g` launches lazygit scoped to the session worktree, no chord collision. — DONE (post-merge, live). lazygit launches scoped to the selected session's worktree (via the home-view `Alt+g` hotkey and the `;` tool picker). No chord collision: AoE checks tool hotkeys before built-in home-screen keybindings (they shadow built-ins), and the audit (1.3) already cleared ghostty/tmux. PREREQUISITE discovered during QA: ghostty did not forward Alt (default `macos-option-as-alt = false` → ⌥ types `©`), so `Alt+g` was inert. Fixed by adding `macos-option-as-alt = right` to `dot_config/ghostty/config` (right ⌥ = Alt; left ⌥ kept for the Spanish/ISO layout's `@ # [ ] \ |`). Also note: the hotkey only fires in the home view with a session selected, NOT while attached to a session.

## 8. TRIAL — gh-dash AoE queue keybindings (f / F)

- [x] 8.1 Add `f` (Queue in AoE) to the `prs:` keybindings: `wt -C {{.RepoPath}} switch pr:{{.PrNumber}} -x 'aoe add . -t "pr {{.RepoName}}#{{.PrNumber}}"'` (no `-l`, no `--trust-hooks`). — Added (plain scalar; YAML valid). Title uses a deterministic `pr repo#N` token instead of `{{.Title}}` (CodeRabbit PR #152): gh-dash renders the template before the shell, so a title with a `'` could break out of the `-x` payload and inject commands.
- [x] 8.2 Add `F` (PR review team). FINAL (revised post-merge QA): `wt -C {{.RepoPath}} switch pr:{{.PrNumber}} -x 'aoe add . -t "review {{.RepoName}}#{{.PrNumber}}" -g "reviews/{{.RepoName}}" -l --extra-args "/review-team {{.RepoName}}#{{.PrNumber}}"'`. Two changes vs the original inline-prompt design: (a) the review prompt now lives in a versioned slash command `dot_claude/commands/review-team.md` (`/review-team`) passed as a SINGLE token via `--extra-args` — the inline `-- "<long prompt>"` truncated to its first word ("Spin") because `aoe add` shell-splits `--extra-args` (it's built for flags); (b) added `-l` so the review auto-launches in background (`aoe add -l` does a background start that returns control to gh-dash) instead of just queuing. Migration to the `experiments` plugin tracked in MON-203 (binding will switch to `/experiments:review-team`).
- [x] 8.3 Confirm both keys are free (no built-in collision), have `name` fields, and gh-dash help shows them. — DONE (post-merge, live). gh-dash `?` help overlay shows `f · Queue in AoE` and `F · Queue AoE review team` in the custom-bindings column alongside `b/i/t/B/I/T`; no collision with built-ins, both have `name` fields.
- [x] 8.4 Smoke test: `f` queues a session and returns to gh-dash; `F` queues a session whose lead spins up the 3-agent team and none post to the PR (verify teammates can invoke the three skills; fallback to sequential single-agent if not). — DONE (post-merge live, both keys confirmed end-to-end). `f`: created session `pr pabloimrik17/dotfiles#97` (deterministic token, not `{{.Title}}`) with the worktree `dotfiles.feature-add-tmux-config` created by worktrunk; control returns to gh-dash. `F`: created session `review pabloimrik17/dotfiles#97` grouped under `reviews/pabloimrik17/dotfiles`, auto-launched (`-l`), and Claude started with the FULL `/review-team` prompt (the 3-agent review instructions). BUG found+fixed during QA: the original inline `-- "<long prompt>"` truncated to "Spin" (aoe shell-splits `--extra-args`); fixed by moving the prompt to the `/review-team` slash command (single token) — see 8.2. The live 3-agent execution + no-PR-post + teammate skill-invocation verification (and the `f` "open later" flow) were folded into the soak (now WAIVED — see §10; both `f`/`F` already proven end-to-end above). `/review-team` migration to the `experiments` plugin tracked in MON-203.

## 9. Sequencing & validation

- [x] 9.1 Resolve the `agent-manager` gate (design D5): archive `agent-management-strategy` (or `/opsx:sync agent-manager`) so the base capability exists in `openspec/specs/` before validating the `agent-manager` delta. — DONE: `agent-management-strategy` is archived (`openspec/changes/archive/2026-06-01-agent-management-strategy`); `agent-manager` is now in `openspec/specs/agent-manager/spec.md` (8 requirements). The delta validates against it (`openspec validate improve-aoe-config --strict` passes).
- [x] 9.2 `openspec validate improve-aoe-config --strict` passes (all three deltas). — PASSES ("Change 'improve-aoe-config' is valid").
- [x] 9.3 `chezmoi diff` shows only the expected changes; re-run installer idempotently (no new installs). — DONE (post-merge). Source synced to HEAD `3af480c`; `chezmoi status` empty and `chezmoi diff ~/.agent-of-empires/config.toml` + `chezmoi diff ~/.claude/settings.json` both QUIET (byte-identical — the 6 managed AoE deltas already on disk, settings.json byte-for-byte). Installer idempotency verified by replaying every pre-scan in `run_onchange_install-packages.sh`: 28/28 brew CLI pkgs present (incl. this change's deps `aoe`/`uv`/`lazygit`/`glow`/`terminal-notifier`), opencode+CodeRabbit on official installers, all 5 Catppuccin themes + tmux plugin, fonts, oh-my-zsh+plugins, MAS apps, NVM/Node v24/corepack, 10/10 CC marketplaces + 25/25 plugins, 13/13 MCP servers up to date → 0 auto-installs. Only non-present items are 6 OPTIONAL GUI casks (firefox, ollama, tailscale-app, adobe-acrobat-reader, whatsapp, microsoft-teams) — interactive-prompt-gated (never auto-installed) and outside this change's scope. run_onchange won't even re-fire (content hash unchanged at synced HEAD).

## 10. Docs & archive

> Soak + per-item verdict (former 10.1/10.2) WAIVED by user decision (2026-07-01): every TRIAL item (modify_, theme, sound, lazygit, f/F bindings) is KEPT as-is — no config or spec reversions. Proceed straight to docs + archive.

- [x] 10.1 Run `/docs:manual` (and `/docs:readme` if tool-level surface changed) for the kept items. — DONE. `docs/manual.html` updated: §1 Terminal added an "Option key" row (right ⌥ = Alt for `Alt+g`, left ⌥ kept for ISO chars); §4 gh-dash added `f` (queue AoE session) + `F` (queue AoE review team) rows; §13 Agent of Empires — theme `tokyo-night-storm`→`catppuccin-mocha`, status hooks now waiting/idle/**error** (Glass/—/Basso), added Sound, lazygit `Alt+g` tool-session, tmux clipboard=enabled + mouse=disabled rows, rewrote the stale "config drift" note to describe the `modify_` tomlkit merge (diff stays quiet), added a "Status column" note for the baked self-gating settings.json hooks. `/docs:readme` NOT needed: no tool-level surface change (aoe/uv/lazygit already shipped; all deltas are config/keybinding-level → manual-only per the readme skill's own rule).
- [x] 10.2 `/opsx:archive improve-aoe-config`. — DONE (2026-07-01): all SHIP + (kept) TRIAL items verified; deltas synced to `openspec/specs/`; change moved to `openspec/changes/archive/`.
