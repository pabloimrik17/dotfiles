## Claim verdicts

| id | claim (abbrev.) | verdict | corrected statement |
|---|---|---|---|
| B1 | "10–25 s migration pause" for v49→v53, CHANGELOG:1207 | CORRECTED | Scoped estimate for migration **0052 only**, on rigs **>10K issues**. Source: `CHANGELOG.md` @ `v1.1.0-rc.1` line 41 (= line 204 @ `v1.1.2`), `### Added`. Not line 1207. The release's own Upgrade Notes flag 0050 (dependencies PK reshape) and 0053 (aux row re-key) as the risky ones, with **no** time estimate. Do not size the upgrade step with 10–25 s. |
| B2a | No hardcoded schema constant; v49→v53 | VERIFIED | `LatestVersion()` derives from embedded migrations (`internal/storage/schema/schema.go:197-202`). v1.0.5 top = `0049_longtext_large_content_columns`; v1.1.2 top = `0053_repair_rig_wisps`. Four new: 0050–0053. |
| B2b | "New hard 10 s `bd prime` timeout" | CORRECTED | `BEADS_PRIME_TIMEOUT`, default 10 s, **not new** — identical constants at v1.0.5 (`cmd/bd/prime.go:34-35`) and v1.1.2 (`:33-34`). Overridable; wraps only the lazy store open in `formatMemoriesForPrime()` (v1.1.2 `prime.go:331-341`), not all of `bd prime`. |
| B2c | "`bd prime` silently swallows errors" | CORRECTED (conclusion stands) | Deadline exceeded *is* surfaced via `formatPrimeMemoryTimeout` (`prime.go:338-343, 388-397`). **Every other** error returns `""` → the `mem != ""` guard (`prime.go:318`) drops the section and prime exits 0. So a failed/half-applied migration or a gate refusal shows up as a **missing memories section**, not an error. |
| B2d | `BD_SMART_GATE` new and default-on | VERIFIED | Absent at v1.0.5; at v1.1.0 `internal/storage/schema/smart_remote_migrate_gate.go:26,52-65` — empty or unparseable env ⇒ enabled. Blunt gate: `BD_ALLOW_REMOTE_MIGRATE` (`remote_migrate_gate.go:18`). Both fire **only** with a remote configured; a purely local `.beads` store is ungated. |
| B3 | v53 aux re-key bricks on 1.1.0; "no 1.1.1 exists" | CORRECTED (substance verified) | 1.1.0 has zero drift handling (`isSchemaEncodingDriftErr` absent); v1.1.2 `internal/storage/schema/aux_row_id_backfill.go:63,154,299-320` skips the drifted table with a warning instead of aborting `MigrateUp` (which previously recurred on every open, leaving the DB readable only by pre-re-key builds). Wording fix: a `v1.1.1` **tag exists**, nothing was published (pipeline package gate failed). Say "no published 1.1.1 release". Net: **target 1.1.2, never 1.1.0**. |
| M1 | mole "matured into a usable iStat-style dashboard" | CORRECTED | No release note supports it. 1.45.0→1.49.2 = safety/reliability/accuracy fixes, no headline feature: fail-safe cleanup on stalled Spotlight/TM probes and protected system caches (1.45.0/1.46.0/1.48.0/1.49.0); `analyze` deletion works over SSH via `trash(8)` (1.48.0/1.48.1); `status` accuracy + rendering fixes (1.46.0/1.47.1/1.48.0/1.49.0); `mo update` fail-closed checksum+attestation (1.47.1–1.49.2); small adds (per-category reclaimed totals, Legacy Overrides, Parallels in `analyze`, full `MOLE_DRY_RUN=1` preview path). Host caveats: Apple-Silicon CPU fix irrelevant (x86_64), macOS 12–14 lock fix irrelevant (15.7.7), `mo update` line inert (Homebrew-managed). |
| M2 | `mole analyze` is not read-only | VERIFIED (anchors corrected) | `cmd/analyze/delete.go:36` `const trashBinary = "/usr/bin/trash"`; `update.go:534,570,574`; `view.go:411` "Press Enter to confirm". **Not new in 1.49.2** — deletion existed at V1.44.1 (`delete.go:34,122-123`, Finder AppleScript); 1.48.0 only swapped in `trash(8)`. So the manual is already wrong for the installed version. Manual anchors are `docs/manual.html:2262-2299` (not :2207-2208/:2180). |
| M2-spec | `openspec/specs/mole-install/spec.md` needs amending | VERIFIED (no change) | Lines 65-73 already forbid all automatic invocation because "mole performs destructive cleanup actions" — that rationale covers `analyze` trashing files. Spec never claims analyze is read-only. `mole status` writing `~/.config/mole/status_prefs` does not violate the no-chezmoi-config requirement (:56-58). |
| M3 | `mole status` worth documenting | VERIFIED | Present already in installed 1.44.1. Covers CPU/memory/disk/battery/network + GPU/Bluetooth/process/health. Genuinely read-only (no `os.Remove`/`RemoveAll`/trash call; `TrashSize` measures only); sole write is its prefs file. Has `-json`, `-watch`, `-interval`. |
| E1 | `--icons` value-eating breaks the aliases | CORRECTED | Mechanism real and a true 0.23.4→0.23.5 regression: `src/options/parser.rs:90-93` declares `--icons` `num_args(0..=1)` + `default_missing_value("auto")` **without** `require_equals(true)`, so `eza --icons foo` exits 2 (`invalid value 'foo'`). Blast radius false: all **nine** (not eight) aliases put `--group-directories-first` right after `--icons`, and clap won't take a leading-hyphen token as a value — `eza --icons --group-directories-first [-la] foo` exits 0 on 0.23.5. Exposure = hand-typed `eza --icons <path>`. Same shape on `--color`, `--classify`, `--hyperlink`, `--absolute`, `--color-scale`. |
| E2 | `--loc`/`--code` need `=` | VERIFIED | `parser.rs:52-57,128-133`: both `num_args(0..=1)` + `require_equals(true)` + `default_missing_value("both")`. `eza -l --loc lines .` → `"lines": No such file or directory`; `--loc=lines` exits 0. Both flags new in 0.23.5. No dotfiles usage. |
| E-theme-fix | 0.23.5 fixes theme.yml color parsing | DROP | No such fix. See Dropped. |
| E-icon-codepoints | Two icon codepoints change | VERIFIED | `src/output/icons.rs`: YAML U+E6A8 → U+E8EB (.yaml/.yml); `.csv` off `Icons::SHEET` → literal U+EEFC (.tsv keeps U+F1C3). No tofu: Hack Nerd Font (`dot_config/ghostty/config:24`) maps both — cmap of the installed `HackNerdFont-Regular.ttf` / `HackNerdFontMono-Regular.ttf` confirms U+E8EB and U+EEFC present. |

## Repo edits this implies

1. `/Users/etherless/WebstormProjects/dotfiles-worktrees/brew-update/docs/manual.html:2296` — replace `<td>Explore disk usage (read-only)</td>` with `<td>Explore disk usage; can move selected items to the Trash (Enter confirms)</td>`.
2. `docs/manual.html:2266-2269` — replace "Destructive actions require explicit confirmation inside the TUI; non-destructive commands (e.g. `<code>mole analyze</code>` and `<code>--dry-run</code>`) only inspect or simulate changes." with: "Destructive actions require explicit confirmation inside the TUI. Only the `<code>--dry-run</code>` variants are guaranteed non-destructive; `<code>mole analyze</code>` is not read-only — it can move selected paths to the Trash after an Enter confirmation. Use `<code>mole status</code>` for a purely read-only view."
3. `docs/manual.html` — add after the `mole analyze` row (after line 2297): `<tr><td><code>mole status</code></td><td>Live system dashboard — CPU, memory, disk, battery, network (read-only; <code>--json</code> for one-shot output)</td></tr>`.
4. `dot_zshrc.tmpl` — **no edit**. All nine `--icons` aliases (lines 206-210, 289-292) are already safe under 0.23.5; do not rewrite to `--icons=auto` on safety grounds. Optionally add an eza note to the manual: under 0.23.5 optional-value flags (`--icons`, `--color`, `--classify`, `--hyperlink`, `--absolute`) need `--flag=value` or a following flag, never `--flag value <path>`; `--loc`/`--code` require the `=` form.
5. Upgrade plan (not a file edit): pin beads to **1.1.2**, never 1.1.0. Add a manual post-upgrade verification step — `bd prime | grep -i 'Persistent Memories'` or `bd doctor` — because the SessionStart (`dot_claude/settings.json.tmpl:180`) and PreCompact (`:159`) hooks run bare `bd prime`, which exits 0 with an empty memories section on any non-timeout store error. No `settings.json.tmpl` edit; the hook command is version-independent.

No edits to `openspec/specs/mole-install/spec.md` or `dot_config/eza/theme.yml`.

## Dropped

- **E-theme-fix** — no theme.yml color-parse fix exists in 0.23.5. `src/theme/ui_styles.rs` (the theme.yml deserializer) is byte-identical v0.23.4 vs v0.23.5; the only `src/theme/` diffs are a let-chain rewrite in `lsc.rs` (parses `LS_COLORS`, not theme.yml) and a `number_prefix`→`unit_prefix` rename in `mod.rs`. Rendering against the repo's `dot_config/eza/theme.yml` is byte-identical on both binaries. Remove the line from the audit.
- **"10–25 s migration pause"** as a whole-upgrade duration — keep only as "0052 index build, one-time 10–25 s on rigs >10K issues".
- **"mole matured into an iStat-style dashboard"** — unsupported; use the M1 wording.
- **"every `--icons` alias is at risk" / "eight aliases"** — false and miscounted; nine aliases, none affected.
- **"no 1.1.1 exists"** — tag exists, release does not.

## Unresolved

- **M1 provenance**: tw93/Mole ships no `CHANGELOG.md` (raw main → 404), so items (a)–(e) rest solely on per-tag GitHub release bodies (`gh api repos/tw93/Mole/releases/tags/<tag>`), not source. Closing it needs per-tag source diffs for the specific cleanup-protection and `mo update` claims, or the audit accepts them as release-note-sourced (label them as such).
- **Actual migration duration on this rig**: no measurement. The only published number is 0052-scoped and >10K-issue-scoped; 0050 and 0053 have none. Closed by `bd stats` (issue count) before upgrading plus timing the first post-upgrade `bd` invocation.
- **0053 behaviour on this specific store**: whether any local table carries the Dolt schema-encoding drift (`gastownhall/beads#4380`) is unknown; 1.1.2 skips and records `aux_row_rekey_drifted` in `local_metadata` rather than failing. Closed post-upgrade by checking that key and the migration log for "aux row id re-key skipped".
- **Marketplace naming**: `dot_claude/settings.json.tmpl:47` points at `steveyegge/beads` while upstream is `gastownhall/beads` (redirects). Not a defect, unverified whether the redirect is permanent — closed by resolving the marketplace URL once at upgrade time.
- **`mole analyze` runtime behaviour** was not executed on this host (source-verified only), deliberately, since it can trash files.