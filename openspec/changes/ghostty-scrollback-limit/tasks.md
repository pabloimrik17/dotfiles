## 1. Add scrollback-limit to Ghostty config

- [ ] 1.1 Add `scrollback-limit = 50000000` with inline comment to the Operational section of `dot_config/ghostty/config`, after `macos-auto-secure-input = true`
