---
description: Spin up a 3-agent team to review the current PR in parallel (none post to the PR; all report in-session)
---

Spin up a team of three agents to review this PR ($ARGUMENTS) in parallel. None of them may post anything on the PR; every agent reports its findings back to me here. Agent 1: run /code-review:code-review. Agent 2: run /code-review. Agent 3: run /verify to confirm the changes actually work. When all three finish, consolidate everything into one summary here.

If agent teams are unavailable in this context, fall back to running the three reviews sequentially yourself and consolidate the results.
