## 1. Config Change

- [x] 1.1 Add inline comment explaining `clipboard-read = ask` security rationale (why `ask` over `allow`, what tools need it)
- [x] 1.2 Add `clipboard-read = ask` to Copy/paste section in `dot_config/ghostty/config`

## 2. Verification

- [x] 2.1 Verify setting is in correct section (after `clipboard-trim-trailing-spaces`)
- [x] 2.2 Verify no duplicate `clipboard-read` entries exist in config
