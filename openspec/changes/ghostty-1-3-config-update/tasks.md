## 1. Window section updates

- [x] 1.1 Add `scrollbar = system` after `window-padding-color = extend`
- [x] 1.2 Replace `window-inherit-working-directory = true` with `tab-inherit-working-directory = true`, `split-inherit-working-directory = true`, `window-inherit-working-directory = true`

## 2. Notifications section

- [x] 2.1 Add new "Notifications" section after "Mouse" section with `notify-on-command-finish = unfocused`, `notify-on-command-finish-action = notify`, and `notify-on-command-finish-after = 10s`
