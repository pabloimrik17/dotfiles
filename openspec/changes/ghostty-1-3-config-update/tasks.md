## 1. Window section updates

- [ ] 1.1 Add `scrollbar = system` after `window-padding-color = extend`
- [ ] 1.2 Replace `window-inherit-working-directory = true` with `tab-inherit-working-directory = true`, `split-inherit-working-directory = true`, `window-inherit-working-directory = true`

## 2. Notifications section

- [ ] 2.1 Add new "Notifications" section after "Mouse" section with `notify-on-command-finish = unfocused` and `notify-on-command-finish-after = 10s`
