# gh-dash configuration — Catppuccin Mocha themed
# https://github.com/dlvhdr/gh-dash

prSections:
    - title: My Pull Requests
      filters: is:open author:@me
    - title: Needs My Review
      filters: is:open review-requested:@me
    - title: Involved
      filters: is:open involves:@me -author:@me

issueSections:
    - title: My Issues
      filters: is:open author:@me
    - title: Assigned
      filters: is:open assignee:@me
    - title: Involved
      filters: is:open involves:@me -author:@me

notificationSections:
    - title: All
      filters: ""
    - title: Review Requested
      filters: reason:review-requested
    - title: Participating
      filters: reason:participating
    - title: Mentions
      filters: reason:mention

theme:
    ui:
        sectionsShowCount: true
        table:
            showSeparator: true
    colors:
        text:
            primary: "#cdd6f4"
            secondary: "#94e2d5"
            inverted: "#1e1e2e"
            faint: "#6c7086"
            warning: "#f38ba8"
            success: "#a6e3a1"
        background:
            selected: "#313244"
        border:
            primary: "#89b4fa"
            secondary: "#a6e3a1"
            faint: "#45475a"

defaults:
    view: prs
    prsLimit: 20
    issuesLimit: 20
    notificationsLimit: 20
    preview:
        open: true
        width: 0.45
    refetchIntervalMinutes: 30

confirmQuit: false
showAuthorIcons: true
smartFilteringAtLaunch: true

keybindings:
    universal:
        - key: g
          name: Lazygit
          command: cd {{.RepoPath}} && lazygit
    prs:
        # Direct execution (suspends TUI)
        - key: C
          name: Code review with Claude
          command: wt -C {{.RepoPath}} switch pr:{{.PrNumber}} -x "claude /code-review:code-review {{.RepoName}}#{{.PrNumber}}"
        - key: W
          name: Worktree + Claude
          command: wt -C {{.RepoPath}} switch pr:{{.PrNumber}} -x claude
        # Tmux variants (opens in new window)
        - key: R
          name: Code review (tmux)
          command: tmux new-window -n "PR-{{.PrNumber}}" 'wt -C {{.RepoPath}} switch pr:{{.PrNumber}} -x "claude /code-review:code-review {{.RepoName}}#{{.PrNumber}}"'
        - key: E
          name: Worktree + Claude (tmux)
          command: tmux new-window -n "PR-{{.PrNumber}}" 'wt -C {{.RepoPath}} switch pr:{{.PrNumber}} -x claude'

pager:
    diff: delta

repoPaths:
    ":owner/:repo": ~/WebstormProjects/:repo
