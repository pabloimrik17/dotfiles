## 1. Rename aliases

- [x] 1.1 In `dot_zshrc.tmpl`, change `alias bd="bun dev"` to `alias bdv="bun dev"`
- [x] 1.2 In `dot_zshrc.tmpl`, change `alias pd="pnpm dev"` to `alias pdv="pnpm dev"`

## 2. Verify

- [x] 2.1 Confirm no other references to the `bd` or `pd` aliases exist in the codebase (scripts, docs, etc.)
- [x] 2.2 Verify `bdv` and `pdv` do not collide with any existing alias or binary
